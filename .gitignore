# Packaged plugin artifact
*.plugin

# promptfoo eval HTML reports (emitted by `promptfoo eval`; uploaded as CI artifacts)
promptfoo-report-*.html

# OS cruft
.DS_Store
.DS_Store?
._*
.Spotlight-V100
.Trashes
Thumbs.db
desktop.ini

# Editor / IDE
.vscode/
.idea/
.claude/
*.swp
*~

# Python (if you add build/eval scripts)
__pycache__/
*.py[cod]
.venv/
venv/

# Node (OpenSpec runs via npx; only present if installed locally)
node_modules/
