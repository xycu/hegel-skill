## 1. References + asserts

- [ ] 1.1 Curate reference answers for a few representative behaviours (EN + PL).
- [ ] 1.2 Add `similar` assertions against them using the #30 embedding provider.

## 2. Gating + verify

- [ ] 2.1 Advisory first; threshold per #30's policy; stay within the CI envelope.
- [ ] 2.2 `./run-tests.sh` green; `openspec validate add-embedding-similarity-asserts --strict` clean.
