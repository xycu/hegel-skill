name: Skill CI

on:
  # PR-only + manual. This is the FAST gate: deterministic lint + the three core
  # behaviours (EN+PL) with the model-graded judge OFF, so a PR finishes in minutes.
  # The full, model-graded suite (all behaviours, both languages, llm-rubric on)
  # runs nightly — see skill-ci-nightly.yml. The whole workflow stays behind a manual
  # approval (the `gate` job's `evals` environment); pushes to main do not run it.
  #
  # No `paths:` filter here (deliberately, #73): `lint` and `core-slm-smoke` are
  # required status checks on `main`, and a required check derived from a
  # workflow-level `paths:` filter never posts — and so never resolves — on a PR that
  # doesn't touch those paths, wedging it forever. Instead every PR runs this
  # workflow, and the `paths-filter` job below decides per-PR whether the gate/lint/
  # smoke jobs do real work or skip themselves (a `skipped` job still satisfies a
  # required check).
  pull_request:
  workflow_dispatch:

concurrency:
  group: skill-ci-${{ github.ref }}
  cancel-in-progress: true

env:
  # Single source of truth for the promptfoo version (matches run-tests.sh).
  PROMPTFOO_VERSION: "0.121.17"
  # The three core behaviours the fast PR gate runs (matches descriptions in
  # promptfoo/tests/<behaviour>.<lang>.yaml, e.g. `en-dialectical`). Nightly runs all.
  CORE_FILTER: "-(dialectical|grief|technical-dismissal)$"

jobs:
  # Job-level path detection (#73). No checkout needed — dorny/paths-filter reads the
  # PR's changed files via the GitHub API. This runs ungated, before `gate`, so an
  # unrelated PR never triggers the `evals` approval prompt: `gate` itself is
  # job-level skipped below when this reports no match.
  paths-filter:
    name: Detect relevant paths
    runs-on: ubuntu-latest
    outputs:
      skill-relevant: ${{ steps.filter.outputs.skill }}
    steps:
      - uses: dorny/paths-filter@v3
        id: filter
        with:
          filters: |
            skill:
              - 'skills/**'
              - 'promptfoo/**'
              - 'tools/**'
              - '.claude-plugin/**'
              - 'run-tests.sh'
              - '.github/workflows/skill-ci.yml'

  # Manual approval checkpoint for the whole workflow. This job uses the `evals`
  # environment (Settings → Environments → evals → Required reviewers), so it sits
  # "Waiting for review" and NOTHING downstream starts until a maintainer approves.
  # Self-approval is allowed (Prevent self-review is off). Every other job
  # `needs: gate`, so one approval releases the entire run.
  #
  # Environment protection rules fire the moment this job is scheduled, before any
  # step runs — a step-level condition can't dodge the approval prompt. So relevance
  # is checked here at the job level: an irrelevant PR skips `gate` outright, which
  # posts a `skipped` status (not `pending`) and never prompts for approval.
  gate:
    name: Approve eval run
    runs-on: ubuntu-latest
    environment: evals
    needs: paths-filter
    # Skip the release-please PR so it never prompts for approval (see lint), and
    # skip entirely when the PR touches none of the watched paths (#73).
    if: ${{ needs.paths-filter.outputs.skill-relevant == 'true' && !startsWith(github.head_ref, 'release-please--') }}
    steps:
      - run: echo "Eval run approved — releasing Skill CI jobs."

  lint:
    name: Deterministic skill lint
    runs-on: ubuntu-latest
    needs: [paths-filter, gate]
    # Skip the release-please PR. It only bumps version fields under
    # .claude-plugin/** (which matches the paths filter), so it would otherwise
    # trigger this workflow — forcing the Actions manual-approval gate and a
    # pointless eval run on a version-only change (#61). On normal PRs head_ref
    # doesn't start with the prefix, so they still run.
    #
    # `lint` is a required status check (#73), so this condition is written as a
    # job-level `if:` rather than per-step: a skip here still posts a `skipped`
    # check under the `lint` job name, which satisfies the requirement instead of
    # leaving it `pending`. Because a custom `if:` replaces the implicit
    # needs-must-succeed check, `needs.gate.result == 'success'` is spelled out so a
    # failed/cancelled `gate` still blocks `lint` from running.
    if: ${{ needs.paths-filter.outputs.skill-relevant == 'true' && !startsWith(github.head_ref, 'release-please--') && needs.gate.result == 'success' }}
    steps:
      - uses: actions/checkout@v5
      - uses: actions/setup-python@v6
        with:
          python-version: "3.12"
      - name: Lint skill package
        run: python tools/skill_lint.py
      - name: Check version fields are in lockstep
        run: python tools/version_check.py
      - name: Check generated install artifacts are current
        # Regenerate every per-tool artifact from the canonical persona and fail
        # on any drift in presence or content (#43). Pairs with the version-parity
        # guard above and the presence guard in skill_lint.py.
        run: python tools/build_install_artifacts.py --check

  core-slm-smoke:
    name: Core SLM smoke (${{ matrix.language }})
    runs-on: ubuntu-latest
    needs: [paths-filter, gate, lint]
    # Also skip on the release-please PR and on an irrelevant PR (see the lint job —
    # same job-level-skip-is-a-satisfied-required-check reasoning applies here).
    #
    # TEMPORARY (revert before merging #135): forced to `false` while iterating on
    # infra-plan's GCP IAM grants, so pushes don't wait ~10 min for the Ollama model
    # pull + eval every time. A job-level skip still posts `skipped` under the same
    # "Core SLM smoke (en)"/"(pl)" names, which satisfies the required check the same
    # way an irrelevant PR does — so this doesn't wedge the PR, it just means the
    # actual eval isn't exercising this branch's changes right now.
    # Real condition: ${{ needs.paths-filter.outputs.skill-relevant == 'true' && !startsWith(github.head_ref, 'release-please--') && needs.lint.result == 'success' }}
    if: false
    # Fast gate: 3 core behaviours, judge off. 30 min is ample headroom; the full
    # graded suite that used to crowd the 90-min envelope now runs nightly.
    timeout-minutes: 30
    # Canary jobs are informational compatibility checks: report pass/fail but
    # never block the build (their models are third-party and may drift).
    continue-on-error: ${{ matrix.canary == true }}
    permissions:
      contents: read
      pull-requests: read  # read PR file list for the eval; the single sticky
                           # results comment is posted by the aggregate job
    strategy:
      fail-fast: false
      matrix:
        include:
          # Behavioural gates: gemma4:e4b-it-qat handles EN and PL. The core configs
          # carry no llm-rubric / similar asserts, so no grader model is pulled.
          - language: en
            display: English
            model: gemma4:e4b-it-qat
            config: promptfoo/promptfooconfig.core.en.yaml
            canary: false
          - language: pl
            display: Polish
            model: gemma4:e4b-it-qat
            config: promptfoo/promptfooconfig.core.pl.yaml
            canary: false
          # Compatibility canary: one Polish case on a Polish-native model
          # (Bielik-7B). Non-blocking. PLLuM was dropped — too terse / breaks
          # character at both 4B and 8B.
          - language: pl-canary-bielik7b
            display: Polish (canary)
            model: hf.co/speakleash/Bielik-Minitron-7B-v3.0-Instruct-GGUF:Q4_K_M
            config: promptfoo/promptfooconfig.core.pl.yaml
            filter: pl-dialectical
            canary: true
    steps:
      - uses: actions/checkout@v5
      - uses: actions/setup-node@v5
        with:
          node-version: "24"
      - name: Install promptfoo
        run: npm install -g promptfoo@"${PROMPTFOO_VERSION}"
      - name: Install Ollama
        run: curl -fsSL https://ollama.com/install.sh | sh
      - name: Start Ollama server
        run: |
          # The install step may already have started a server; only launch one
          # if the port is free, else `ollama serve` logs "address already in use".
          if ! curl -sf http://localhost:11434/api/tags >/dev/null 2>&1; then
            ollama serve &
          fi
          for i in $(seq 1 30); do
            curl -sf http://localhost:11434/api/tags >/dev/null && break
            sleep 1
          done
      - name: Pull model
        run: ollama pull ${{ matrix.model }}
      # Behavioural gate (en/pl): run promptfoo directly. `-j 1` serves one request
      # at a time so the single CPU-bound Ollama doesn't starve under parallelism
      # (#47); REQUEST_TIMEOUT_MS raises the per-call timeout as margin. The core
      # config has no model-graded asserts, so this is generation + keyword checks
      # only. `--filter-pattern` narrows to the three core behaviours. `promptfoo
      # eval` exits non-zero on any non-pass, so this stays a hard 100% gate.
      - name: Run evals (behavioural gate)
        if: ${{ matrix.canary != true }}
        env:
          EVAL_MODEL: ${{ matrix.model }}
          REQUEST_TIMEOUT_MS: "600000"
        run: promptfoo eval -c ${{ matrix.config }} --no-cache -j 1 --filter-pattern "${CORE_FILTER}"
      # Canary: filtered single case on a third-party model; non-blocking, no comment.
      - name: Run canary eval
        if: ${{ matrix.canary == true }}
        env:
          EVAL_MODEL: ${{ matrix.model }}
        run: promptfoo eval -c ${{ matrix.config }} --no-cache --filter-pattern ${{ matrix.filter }} --verbose
      # Render the just-run eval to a self-contained HTML report from promptfoo's
      # local store. `always()` so a failed eval still produces it.
      - name: Render HTML report
        if: always()
        run: promptfoo export eval latest -o promptfoo-report-${{ matrix.language }}.html
      - name: Upload eval report
        if: always()
        uses: actions/upload-artifact@v7
        with:
          name: promptfoo-report-${{ matrix.language }}
          path: promptfoo-report-${{ matrix.language }}.html
          if-no-files-found: warn
      # Extract a compact pass/fail summary from the same stored eval so the
      # aggregate job can assemble one combined PR comment. `always()` so a failed
      # gate still reports its counts.
      - name: Summarize result
        if: always()
        env:
          DISPLAY: ${{ matrix.display }}
          MODEL: ${{ matrix.model }}
          BLOCKING: ${{ matrix.canary != true }}
        run: |
          if promptfoo export eval latest -o eval.json 2>/dev/null; then
            passes=$(jq '.results.stats.successes // 0' eval.json)
            failures=$(jq '.results.stats.failures // 0' eval.json)
            errors=$(jq '.results.stats.errors // 0' eval.json)
          else
            passes=0; failures=0; errors=0
          fi
          total=$((passes + failures + errors))
          jq -n \
            --arg label "$DISPLAY" --arg model "$MODEL" \
            --argjson passes "$passes" --argjson failures "$failures" \
            --argjson errors "$errors" --argjson total "$total" \
            --argjson blocking "$BLOCKING" \
            '{label:$label, model:$model, passes:$passes, failures:$failures, errors:$errors, total:$total, blocking:$blocking}' \
            | tee "summary-${{ matrix.language }}.json"
      - name: Upload result summary
        if: always()
        uses: actions/upload-artifact@v7
        with:
          name: eval-summary-${{ matrix.language }}
          path: summary-${{ matrix.language }}.json
          if-no-files-found: warn

  # Aggregate every matrix entry's summary into ONE sticky PR comment that updates
  # in place across pushes (keyed by a hidden marker), so comments never pile up.
  # `always()` so it still reports when a gate fails; PR-only since it comments.
  aggregate-eval-comment:
    name: Eval results comment
    runs-on: ubuntu-latest
    needs: core-slm-smoke
    # `always()` so a failed gate still gets a comment, but not when core-slm-smoke was
    # itself skipped (irrelevant PR, #73) — otherwise every unrelated PR would get a
    # noisy "no eval summaries were produced" fallback comment.
    if: ${{ always() && github.event_name == 'pull_request' && !startsWith(github.head_ref, 'release-please--') && needs.core-slm-smoke.result != 'skipped' }}
    permissions:
      contents: read
      pull-requests: write  # post / update the single sticky results comment
    steps:
      - name: Download result summaries
        # Tolerate zero matches (e.g. every smoke leg died before uploading) so the
        # next step can still render the "no summaries" fallback comment.
        continue-on-error: true
        uses: actions/download-artifact@v7
        with:
          pattern: eval-summary-*
          merge-multiple: true
          path: summaries
      - name: Build and post sticky comment
        env:
          GH_TOKEN: ${{ secrets.GITHUB_TOKEN }}
          REPO: ${{ github.repository }}
          PR: ${{ github.event.pull_request.number }}
          HEAD_SHA: ${{ github.event.pull_request.head.sha }}
          RUN_URL: ${{ github.server_url }}/${{ github.repository }}/actions/runs/${{ github.run_id }}
        run: |
          set -euo pipefail
          marker='<!-- skill-ci-eval-results -->'
          shopt -s nullglob
          files=(summaries/*.json)
          if [ ${#files[@]} -eq 0 ]; then
            table="_No eval summaries were produced — the smoke jobs likely failed before running. See the [workflow run]($RUN_URL)._"
            overall='⚠️ unknown'
          else
            arr=$(jq -s 'sort_by([(.blocking | not), .label])' "${files[@]}")
            # Model column shows just the model name, not its full hf.co/org/ path.
            rows=$(jq -r '
              def passed: .total > 0 and .failures == 0 and .errors == 0;
              .[] |
              (if passed then "✅ pass" else "❌ fail" end) as $res |
              (.model | split("/") | last) as $model |
              (if .errors > 0 then " (\(.errors) err)" else "" end) as $err |
              "| \(.label) | `\($model)` | \($res) | \(.passes)/\(.total)\($err) |"
            ' <<<"$arr")
            table=$(printf '| Suite | Model | Result | Cases |\n|---|---|---|---|\n%s' "$rows")
            if jq -e 'def passed: .total > 0 and .failures == 0 and .errors == 0;
                      all(.[]; (.blocking | not) or passed)' <<<"$arr" >/dev/null; then
              overall='✅ pass'
            else
              overall='❌ fail'
            fi
          fi
          body=$(printf '%s\n### 🧪 Core SLM smoke (fast PR gate)\n\nThe three core behaviours (dialectical, grief, technical-dismissal) run against local models via Ollama (promptfoo), **keyword gates only — no model-graded judge**. Behavioural **gates** must pass every case (100%%); **canaries** are third-party-model compatibility checks and never block. The full model-graded suite (all behaviours, both languages, llm-rubric on) runs nightly.\n\n%s\n\n**Gate status: %s** — commit `%s`. Per-language HTML reports and full logs are in the [workflow run](%s) → Artifacts.\n' \
            "$marker" "$table" "$overall" "${HEAD_SHA:0:7}" "$RUN_URL")
          # Find a prior sticky comment by its hidden marker. Capture the full API
          # result first, then pick the first id — piping --paginate straight into
          # head would SIGPIPE the API call and trip `pipefail`.
          ids=$(gh api "repos/$REPO/issues/$PR/comments" --paginate \
            --jq ".[] | select(.body | contains(\"$marker\")) | .id")
          existing=$(printf '%s\n' "$ids" | head -n1)
          if [ -n "$existing" ]; then
            gh api -X PATCH "repos/$REPO/issues/comments/$existing" -f body="$body" >/dev/null
            echo "Updated sticky comment $existing"
          else
            gh api -X POST "repos/$REPO/issues/$PR/comments" -f body="$body" >/dev/null
            echo "Created sticky comment"
          fi
