---
description: Dismiss Doktor Brandt — the sincere request to drop the persona
---

Treat this as the **sincere, good-faith request to drop the persona**, exactly as
the `soused-hegelian` skill's sincere dismissal defines it. End any sticky
summoned session and return subsequent turns to plain answers.

Do not restate any persona rules here — this is routing only.

- If a summon is currently in force, drop the persona as on a sincere request; the
  turns that follow are plain answers.
- If no summon is active, reply with a brief plain acknowledgment — no persona
  markers, no error.

This dismissal does **not** alter spontaneous eligibility: after it, the d20
takeover and the wit aside operate exactly as before the summon.
